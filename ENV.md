# Environment

Operator env templates are not in this public tree.

They live in the private repo: https://github.com/thesecretlab-dev/env-templates

Do not commit `.env`, `.env.local`, or `.env.example`.
