# ============================================================================
# ZeroID — Environment Configuration
# © thesecretlab | "Verify the human. Drop the liability."
# ============================================================================

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development

# API Keys (comma-separated)
ZEROID_API_KEYS=zid_dev_testkey123

# CORS
ZEROID_CORS_ORIGIN=*

# Issuer EdDSA private key (32-byte hex; leave empty to auto-generate for dev)
# ZEROID_ISSUER_PRIVATE_KEY=

# Keys directory (default: ./.keys)
# ZEROID_KEYS_DIR=./.keys

# Verification key path (generated by circuits/scripts/setup.sh)
# ZEROID_VKEY_PATH=./circuits/setup/verification_key.json

# Regulator encryption keys (per regulator ID)
# ZEROID_REGULATOR_KEY_DEFAULT=<64-char-hex>

# KYC Provider (mock | onfido)
ZEROID_KYC_PROVIDER=mock

# ── OrbitDB / IPFS ──
# Storage directory for OrbitDB data
ZEROID_ORBITDB_DIR=./orbitdb/zeroid
# libp2p TCP listen address
ZEROID_IPFS_LISTEN=/ip4/0.0.0.0/tcp/4002
# Bootstrap peer multiaddrs (comma-separated)
# ZEROID_BOOTSTRAP_ADDRS=/ip4/1.2.3.4/tcp/4002/p2p/QmPeerID
# Authorized peer IDs for replication (comma-separated; empty = allow all)
# ZEROID_AUTHORIZED_PEERS=QmPeer1,QmPeer2
# Master key for per-store encryption key derivation (64-char hex)
# In production, load from a vault (AWS KMS, HashiCorp Vault)
# ZEROID_STORE_MASTER_KEY=<64-char-hex>
